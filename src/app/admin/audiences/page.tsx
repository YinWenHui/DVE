import { AudienceManager } from "@/components/admin/audience-manager";
import { getMockStore } from "@/lib/mock-store";
export default function AudiencesPage() { const store = getMockStore(); return <><div className="page-heading"><div><p className="eyebrow">Authorization</p><h1>Audiences</h1><p>Control report visibility by role or explicitly assigned user.</p></div></div><AudienceManager apps={store.apps} reports={store.reports} /><section className="panel"><table className="admin-table"><thead><tr><th>Application</th><th>Audience</th><th>Roles</th><th>Users</th><th>Visible reports</th></tr></thead><tbody>{store.apps.flatMap((app) => app.audiences.map((audience) => <tr key={audience.id}><td>{app.name}</td><td><strong>{audience.name}</strong></td><td>{audience.roles.join(", ")}</td><td>{audience.userIds.length || "Role based"}</td><td>{audience.reportIds.length}</td></tr>))}</tbody></table></section></>; }
