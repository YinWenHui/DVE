import { getMockStore } from "@/lib/mock-store";
export default function AuditPage() { const records = getMockStore().audit; return <><div className="page-heading"><div><p className="eyebrow">Traceability</p><h1>Audit log</h1><p>Administrative, authentication, refresh, integration, and writeback events.</p></div></div><section className="panel"><table className="admin-table"><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>User / process</th><th>Details</th></tr></thead><tbody>{records.map((record) => <tr key={record.id}><td>{new Date(record.createdAt).toLocaleString()}</td><td><code>{record.action}</code></td><td>{record.entityType}<small>{record.entityId}</small></td><td>{record.userDisplayName}</td><td>{record.details}</td></tr>)}</tbody></table></section></>; }
