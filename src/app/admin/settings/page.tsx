import { config } from "@/lib/config";
export default function SettingsPage() { return <><div className="page-heading"><div><p className="eyebrow">Configuration</p><h1>System settings</h1><p>Environment-backed defaults for runtime, refresh, freshness, and security.</p></div></div><section className="panel"><div className="panel-header"><div><h2>Runtime mode</h2><p>Sensitive settings are environment variables and are never returned through APIs.</p></div></div><div className="form-grid"><label>Application timezone<input value={config.APP_TIMEZONE} readOnly /></label><label>Authentication mode<input value={config.AUTH_MODE} readOnly /></label><label>Data mode<input value={config.DATA_MODE} readOnly /></label><label>Session lifetime<input value={`${config.SESSION_TTL_MINUTES} minutes`} readOnly /></label><label>Maximum upload<input value={`${config.DVE_MAX_UPLOAD_MB} MB`} readOnly /></label><label>Maximum export rows<input value={config.DVE_MAX_EXPORT_ROWS.toLocaleString()} readOnly /></label></div></section><section className="panel"><div className="panel-header"><div><h2>Default freshness thresholds</h2><p>Dataset-specific values can override these defaults.</p></div></div><div className="form-grid"><label>Refresh interval<input value="5 minutes" readOnly /></label><label>Delayed after<input value="7 minutes" readOnly /></label><label>Stale after<input value="10 minutes" readOnly /></label><label>Timestamp storage<input value="UTC (displayed in Asia/Bangkok)" readOnly /></label></div></section><section className="panel"><h2>Phase 1 boundaries</h2><p className="muted">Single-table semantic datasets only. PBIX import, arbitrary DAX, raw SQL from browsers, production source correction, and Microsoft Graph integration are intentionally unavailable.</p></section></>; }
