import Link from "next/link";
import { Edit3, Plus } from "lucide-react";
import { getMockStore } from "@/lib/mock-store";

export default function ApplicationsPage() { const store = getMockStore(); return <><div className="page-heading"><div><p className="eyebrow">Distribution</p><h1>Applications</h1><p>Group reports into published experiences with sections and audiences.</p></div><Link className="button primary" href="/admin/apps/new"><Plus size={16} /> New application</Link></div><section className="panel"><table className="admin-table"><thead><tr><th>Application</th><th>Status</th><th>Sections</th><th>Reports</th><th>Audiences</th><th>Published</th><th>Actions</th></tr></thead><tbody>{store.apps.map((app) => <tr key={app.id}><td><strong>{app.name}</strong><small>/{app.slug}</small></td><td><span className={`badge ${app.status}`}>{app.status}</span></td><td>{app.sections.length}</td><td>{app.sections.reduce((count, section) => count + section.reportIds.length, 0)}</td><td>{app.audiences.length}</td><td>{app.publishedAt ? new Date(app.publishedAt).toLocaleString() : "—"}</td><td><Link className="icon-button" href={`/admin/apps/${app.id}/edit`}><Edit3 size={15} /></Link></td></tr>)}</tbody></table></section></>; }
