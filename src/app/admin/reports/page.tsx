import Link from "next/link";
import { Edit3, Plus } from "lucide-react";
import { getMockStore } from "@/lib/mock-store";

export default function ReportsPage() { const store = getMockStore(); return <><div className="page-heading"><div><p className="eyebrow">Content authoring</p><h1>Reports</h1><p>Metadata-driven pages and visuals built on one governed dataset.</p></div><Link className="button primary" href="/admin/reports/new"><Plus size={16} /> New report</Link></div><section className="panel"><table className="admin-table"><thead><tr><th>Report</th><th>Dataset</th><th>Pages</th><th>Visuals</th><th>Minimum role</th><th>Status</th><th>Actions</th></tr></thead><tbody>{store.reports.map((report) => <tr key={report.id}><td><strong>{report.name}</strong><small>{report.description}</small></td><td>{store.datasets.find((item) => item.id === report.datasetId)?.name}</td><td>{report.pages.length}</td><td>{report.pages.reduce((count, page) => count + page.visuals.length, 0)}</td><td>{report.minimumRole}</td><td><span className={`badge ${report.status}`}>{report.status}</span></td><td><Link className="icon-button" href={`/admin/reports/${report.id}/edit`}><Edit3 size={15} /></Link></td></tr>)}</tbody></table></section></>; }
