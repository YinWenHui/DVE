import { RefreshMonitor } from "@/components/admin/refresh-monitor";
import { getMockStore } from "@/lib/mock-store";
export default function RefreshPage() { const store = getMockStore(); return <><div className="page-heading"><div><p className="eyebrow">Operations</p><h1>Refresh monitor</h1><p>Independent worker schedules, run status, and previous-version protection.</p></div><span className="badge healthy">Worker architecture ready</span></div><RefreshMonitor initialDatasets={store.datasets} /><section className="panel"><div className="panel-header"><div><h2>Recent refresh runs</h2><p>Manual, interval, and future cron triggers share the same versioned import path.</p></div></div>{store.refreshRuns.length ? <table className="admin-table"><thead><tr><th>Run</th><th>Dataset</th><th>Status</th><th>Started</th><th>Rows</th></tr></thead><tbody>{store.refreshRuns.map((run) => <tr key={run.id}><td><code>{run.id.slice(0, 8)}</code></td><td>{run.datasetId}</td><td><span className={`badge ${run.status}`}>{run.status}</span></td><td>{new Date(run.startedAt).toLocaleString()}</td><td>{run.rowsWritten ?? "—"}</td></tr>)}</tbody></table> : <div className="empty-state">No refresh runs in this process yet. Use Refresh now to create one.</div>}</section></>; }
